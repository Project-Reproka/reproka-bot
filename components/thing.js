function alternateNameLmao(client) {
  console.log('> just an empty shell of a reproka bot...')
  console.log('> lets see what kind of stuff we add later')
}

module.exports = {callThisWhenStarted: alternateNameLmao}